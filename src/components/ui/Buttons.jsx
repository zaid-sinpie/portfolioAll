import React from 'react'

const Buttons = ({children}) => {
  return (
    <button className='bg-actionColor px-4 py-2 rounded-md font-bold'>{children}</button>
  )
}

export default Buttons
// rafce create auto function